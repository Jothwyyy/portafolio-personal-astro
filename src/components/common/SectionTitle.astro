---
interface Props {
	headerType: string,
	title: string;
	description?: string;
}

const {
	headerType,
	title,
	description,
} = Astro.props;
---

<header class={headerType}>
	<h2 set:html={title} />
	{description && <p set:html={description} />}
</header>
