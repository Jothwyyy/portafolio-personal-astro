---
interface Props {
	title: string;
	description?: string;
}

const {
	title,
	description,
} = Astro.props;
---

<header class="section-title">
	<h2>{title}</h2>
	{description && <p>{description}</p>}
</header>
