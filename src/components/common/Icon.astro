---
interface Props {
	name: string;
	label?: string;
}

const {
	name,
	label = name,
} = Astro.props;
---

<span class={`icon ${name}`} aria-label={label}>
	<span class="label">{label}</span>
</span>
